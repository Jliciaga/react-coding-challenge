import * as React from "react";

export interface TextProps {}

const Text: React.FC<TextProps> = ({}): React.ReactElement => {
    return (
        <div>
            <h1>Hello World - Text</h1>
        </div>
    );
};

export default Text;
