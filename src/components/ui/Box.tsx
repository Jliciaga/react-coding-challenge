import * as React from "react";

export interface BoxProps {}

const Box: React.FC<BoxProps> = ({}): React.ReactElement => {
    return (
        <div>
            <h1>Hello World - Box</h1>
        </div>
    );
};

export default Box;
