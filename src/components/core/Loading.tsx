import * as React from "react";

export interface LoadingProps {}

const Loading: React.FC<LoadingProps> = ({}): React.ReactElement => {
    return (
        <div>
            <div>Hello World - Loading</div>
        </div>
    );
};

export default Loading;
